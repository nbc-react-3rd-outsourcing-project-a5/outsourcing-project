const color = {
  white: '#ffffff',
  black: '#252525'
};

const fontSize = {
  sm: '0.8rem',
  default: '1rem',
  lg: '1.2rem'
};

const spacing = {
  default: '1rem'
};

const theme = {
  color,
  fontSize,
  spacing
};

export default theme;
