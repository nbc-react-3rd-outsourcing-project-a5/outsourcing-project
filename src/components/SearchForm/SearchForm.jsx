import React from 'react';
import styled from 'styled-components';

export default function SearchForm() {
  return (
    <div></div>
    // 검색폼 구조 설계

    // 1. form 태그 -> onSubmit
    // 2. select, option : 기간, 지역
    // 3. input : 검색창 -> useState, onChange
    // 4. button : 검색
    // filter, includes : 필터링 기능으로 검색어만 도출하기
    //
  );
}
